@model IEnumerable<IdentityRole>

<div class="text-center">
    <div class="display-6 my-5">Roles</div>
</div>

<ul class="list-group">
    @foreach(var role in Model)
    {
        <li class="list-group-item my-5">@role.Name</li>
    }
</ul>